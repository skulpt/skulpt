// Needed because files are just being concatentated

const fs = require('fs');

require('google-closure-library');
goog.require('goog.string');
goog.require('goog.debug.Error');
goog.require('goog.asserts');

strftime = require('strftime');
